# Single OpenAI API key used by the whole pipeline.
OPENAI_API_KEY=

# Base URL of the Cognee Docker REST service (add / cognify / search).
# Run locally with: docker compose up --build cognee
# (see https://docs.cognee.ai/guides/deploy-rest-api-server)
COGNEE_API_URL=http://localhost:8000
