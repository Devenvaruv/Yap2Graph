# Cognee uses this key only for local graph construction and retrieval.
# The service itself stores all data under cognee-service/.cognee/.
LLM_PROVIDER=openai
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
EMBEDDING_PROVIDER=openai
EMBEDDING_API_KEY=
EMBEDDING_MODEL=text-embedding-3-small
